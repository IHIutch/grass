name: CI
on:
  push:
    branches:
      - master
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - name: version info
        run: rustc --version; cargo --version;

      - name: Run all tests
        run: cargo test --features=macro

  fmt:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - run: |
          rustup component add rustfmt
          cargo fmt --all -- --check

  clippy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - run: |
          rustup component add clippy
          cargo clippy --features=macro -- -D warnings

  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - name: Build
        run: cargo build

      - name: Install dart-sass 1.97.3
        run: |
          wget https://github.com/sass/dart-sass/releases/download/1.97.3/dart-sass-1.97.3-linux-x64.tar.gz
          tar -xzvf dart-sass-1.97.3-linux-x64.tar.gz

      - name: Install bootstrap
        run: git clone --depth=1 --branch v5.0.2 https://github.com/twbs/bootstrap.git

      - name: Verify unchanged output
        run: |
          ./target/debug/grass bootstrap/scss/bootstrap.scss > grass-output.css
          ./dart-sass/sass bootstrap/scss/bootstrap.scss > dart-sass-output.css

          DIFF_LINES=$(diff -u grass-output.css dart-sass-output.css | grep "^[+-]" | grep -v "^[+-][+-][+-]" | wc -l)
          TOTAL_LINES=$(wc -l < dart-sass-output.css)

          if [ "$DIFF_LINES" -eq 0 ]; then
              echo "No differences found"
          elif [ "$DIFF_LINES" -lt 200 ]; then
              echo "Minor differences found ($DIFF_LINES changed lines out of $TOTAL_LINES — likely color rounding)"
              diff -u grass-output.css dart-sass-output.css | head -40
          else
              echo "Significant differences found ($DIFF_LINES changed lines)"
              diff -u grass-output.css dart-sass-output.css
              exit 1
          fi

  sass-spec:
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - name: Build binary
        run: cargo build --release

      - name: Get sass-spec
        run: git submodule init && git submodule update

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install sass-spec dependencies
        run: cd sass-spec && npm install

      - name: Run sass-spec
        run: cd sass-spec && npm run sass-spec -- -c '../target/release/grass' --impl grass
